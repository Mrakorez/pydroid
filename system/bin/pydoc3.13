#!/system/bin/sh

MODDIR="/data/adb/modules/pydroid"

export PATH="${MODDIR}/usr/bin:${PATH}"
export LD_LIBRARY_PATH="${MODDIR}/usr/lib:${LD_LIBRARY_PATH}"

[ ! "${TMPDIR}" ] &&
  export TMPDIR="${MODDIR}/usr/tmp"

pydoc3.13 "$@"
